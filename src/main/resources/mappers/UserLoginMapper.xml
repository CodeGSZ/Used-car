<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.crs_java.mapper.UserLoginMapper">

    <!--用户登录-->
    <select id="Login" resultType="com.example.crs_java.entity.User" >
        select * from user where name = #{name}
    </select>

    <update id="UpdataNP">
        update user set name = #{name}, password = #{password} where id = #{id};
    </update>

    <update id="UpdateAddress">
        update user set address = #{address} where id = #{id}
    </update>

    <update id="addShoping">
        update user set orderNum = #{num} where id = #{id}
    </update>

</mapper>