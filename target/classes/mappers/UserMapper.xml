<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.crs_java.mapper.UserAddMapper">
    <!--使用ResultMap进行字段映射-->
<!--    <resultMap id="test" type="com.example.crs_java.entity.User">-->
<!--        <result column="" property="" />-->
<!--    </resultMap>-->

    <!--用户注册-->
    <insert id="AddUser" useGeneratedKeys="true" keyProperty="id">
        insert into user (name, password,tel,sex,birthday,orderNum,Identy) values (
             #{name},#{password},#{tel},#{sex},#{birthday},#{orderNum},#{Identy}
         )
    </insert>

<!--    &lt;!&ndash;用户登录&ndash;&gt;-->
<!--    <select id="Login" parameterType="com.example.crs_java.mapper.UserLoginMapper">-->
<!--        select * from user where name=#{name}-->
<!--    </select>-->

</mapper>